apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-keycloak
  labels:
    app: ecommerce-keycloak
spec:
  selector:
    matchLabels:
      app: ecommerce-keycloak
  template:
    metadata:
      labels:
        app: ecommerce-keycloak
    spec:
      containers:
        - name: ecommerce-keycloak
          image: quay.io/keycloak/keycloak:26.2.3
          args:
            - "start-dev"
            - "--import-realm"
          ports:
            - containerPort: 8080
          env:
            - name: KEYCLOAK_ADMIN
              value: admin
            - name: KEYCLOAK_ADMIN_PASSWORD
              value: secret
          startupProbe:
            httpGet:
              path: /realms/master
              port: 8080
            failureThreshold: 40
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /realms/master
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 350Mi
            limits:
              cpu: 500m
              memory: 750Mi
          volumeMounts:
            - name: ecommerce-keycloak-config-volume
              mountPath: /opt/keycloak/data/import
      volumes:
        - name: ecommerce-keycloak-config-volume
          configMap:
            name: ecommerce-keycloak

---

apiVersion: v1
kind: Service
metadata:
  name: ecommerce-keycloak
  labels:
    app: ecommerce-keycloak
spec:
  type: ClusterIP
  selector:
    app: ecommerce-keycloak
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: keycloak-ingress
  labels:
    app: ecommerce-keycloak
spec:
  rules:
    - host: ecommerce-keycloak
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ecommerce-keycloak
                port:
                  number: 80